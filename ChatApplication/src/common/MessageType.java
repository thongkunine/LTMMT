package common;

public enum MessageType {
    LOGIN,
    LOGIN_OK,
    LOGIN_FAIL,
    CHAT,
    JOIN,
    LEAVE,
    SYSTEM,
    CREATE_ROOM,
    JOIN_ROOM,
    ROOM_LIST,
    USER_LIST,
    HISTORY
}
